const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    host: 'localhost',
    user: 'root',
    password: '123456',
    port: '3306',
    database: 'order'
  }
}

module.exports = config
